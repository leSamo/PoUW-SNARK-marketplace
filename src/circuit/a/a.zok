/*
    Prove a knowledge of two factors which make up product.

    Number of constraints: 3
*/

def main(public field factor1, public field factor2, public field product, public field integrity) -> bool {
    assert (integrity != 0);

    assert (factor1 * factor2 == product);
    return true;
}
