
/*
    Prove a knowledge of number which equals to 16 when squared.

    Number of constraints: 54
*/

def main(private u16 x) -> bool {
    u16 x_squared = x * x;
    u16 target = 16;

    return x_squared == target;
}
